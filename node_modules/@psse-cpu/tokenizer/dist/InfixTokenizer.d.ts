export default class InfixTokenizer {
    private expression;
    private regex;
    private nextToken?;
    constructor(expression: string, functions?: string[]);
    hasMoreTokens(): boolean;
    readToken(): string | undefined;
}
